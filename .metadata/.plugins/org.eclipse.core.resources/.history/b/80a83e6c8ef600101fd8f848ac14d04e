server:
  port: 8484   # unique port per service

spring:
  application:
    name: POLICY-SERVICE
		  # IMPORTANT: unique name

eureka:
  client:
    service-url:
      defaultZone: http://localhost:8761/eureka/
